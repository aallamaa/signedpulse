# Rust build artifacts
/target

# Local/real configs that may hold secrets (examples/*.toml are fine)
/etc/
*.state.json
*.state.pid

# Secrets — crates.io tokens, keys (never commit these)
crate_token.txt
*token*.txt
*.key
*.pem

# Local tooling state
/.claude/settings.local.json

# Editor/OS cruft
*.swp
.DS_Store
